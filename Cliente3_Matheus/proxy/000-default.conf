<VirtualHost *:80>

	ServerName portal.cliente3.br

	RewriteEngine On

	ProxyPreserveHost On
    ProxyRequests off
    ProxyAddHeaders On

	RewriteRule ^/(.*)$ http://portal_c3:80/$1 [P]

</VirtualHost>

<VirtualHost *:80>

	ServerName php.cliente2.br

	RewriteEngine On
	
	ProxyAddHeaders On
	ProxyPreserveHost On
	ProxyRequests Off

	RewriteRule ^/(.*)$ http://phpmyadmin2:80/$1 [P]

</VirtualHost>

<VirtualHost *:80>

	ServerName local.cliente2.br

	RewriteEngine On
	
	ProxyAddHeaders On
	ProxyPreserveHost On
	ProxyRequests Off

	RewriteRule ^/(.*)$ http://portal_c3:80/$1 [P,QSA,L]

</VirtualHost>