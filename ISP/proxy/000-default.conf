<VirtualHost *:80>

	ServerName portal.bye.br
	ServerAlias *.bye.br

	RewriteEngine On
	RewriteRule ^(.*)$ https://%{HTTP_HOST}%{HTTP_REQUEST} [R=301,L]

</VirtualHost>

<VirtualHost *:80>

	ServerName portal.cliente1.br
	ServerAlias *.cliente1.br

	RewriteEngine On
	#RewriteRule ^(.*)$ https://%{HTTP_HOST}%{HTTP_REQUEST} [R=301,L]

	ProxyPass / http://proxy_c1:80/
	ProxyPassReverse / http://proxy_c1:80/

</VirtualHost>

<VirtualHost *:80>

	ServerName portal.cliente2.br
	ServerAlias *.cliente2.br

	RewriteEngine On
	#RewriteRule ^(.*)$ https://%{HTTP_HOST}%{HTTP_REQUEST} [R=301,L]
	#Redirect permanent / https://*.cliente2.br/
	ProxyPass / http://proxy_c2:80/
	ProxyPassReverse / http://proxy_c2:80/

</VirtualHost>

<VirtualHost *:80>

	ServerName c1.cliente3.br
	ServerAlias *.cliente3.br
	
	#RewriteRule ^(.*)$ https://%{HTTP_HOST}%{HTTP_REQUEST} [R=301,L]

	ProxyPass / http://proxy_c3:80/
	ProxyPassReverse / http://proxy_c:80/

</VirtualHost>
